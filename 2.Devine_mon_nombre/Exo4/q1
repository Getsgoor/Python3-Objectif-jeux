from random import randint


nMax = int(input("nombre maximum ? "))
print()

for i in range(10) :
    a = randint(1, nMax)
    b = randint(1,nMax)
    bonneReponse = False
    
    while (not bonneReponse) :
        reponse = int(input(f"{a} x {b} = "))
        bonneReponse = reponse == a * b
        if (not bonneReponse) :
            print("Faux ! Réessayez !")

